@typeparam TEnum

<MudSelect T="TEnum">
    @{
        foreach (TEnum enumValue in Enum.GetValues(typeof(TEnum)))
        {
            <MudSelectItem T="TEnum">@enumValue</MudSelectItem>
        }
    }
</MudSelect>